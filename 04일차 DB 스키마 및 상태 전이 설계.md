# 4일차 — DB 스키마 및 상태 전이 설계

## 1. 설계 목표

AutoSites의 회원, 고객 조직, 홈페이지, 페이지, 콘텐츠, 버전, 생성 작업, Preview, 배포 및 변경 이력을 안전하게 저장할 데이터 구조를 정의한다.

### 설계 원칙

- 기본키는 외부에서 추측하기 어려운 UUID 계열 식별자를 사용한다.
- 고객 소유 데이터에는 반드시 `tenant_id`를 포함한다.
- 클라이언트가 전달한 `tenant_id`는 신뢰하지 않는다.
- 삭제가 운영·감사에 영향을 주는 데이터는 우선 논리 삭제한다.
- Draft는 수정 가능하지만 Version은 불변 스냅샷으로 저장한다.
- 게시와 롤백은 `published_version_id`를 원자적으로 변경한다.
- 생성·QA·게시 작업에는 멱등성 키와 재시도 정보를 저장한다.
- 모든 시각은 UTC로 저장하고 화면에서 사용자 시간대로 변환한다.

## 2. 전체 ERD

```text
tenants
├─ tenant_members ─ users
├─ sites
│  ├─ site_settings
│  ├─ business_profiles
│  ├─ pages
│  │  └─ sections
│  ├─ assets
│  ├─ site_versions
│  ├─ preview_tokens
│  ├─ site_domains
│  ├─ jobs
│  │  └─ job_attempts
│  └─ deployments
└─ audit_logs

templates
└─ template_versions
```

결제·구독 테이블은 5·6차 개발 단계에서 별도 확장한다.

## 3. 공통 컬럼 규칙

| 컬럼 | 형식 | 용도 |
|---|---|---|
| `id` | UUID | 기본키 |
| `tenant_id` | UUID | 고객 조직 데이터 경계 |
| `created_at` | timestamptz | 생성 시각 |
| `updated_at` | timestamptz | 마지막 수정 시각 |
| `deleted_at` | timestamptz, nullable | 논리 삭제 시각 |
| `created_by` | UUID, nullable | 생성 사용자 |
| `updated_by` | UUID, nullable | 마지막 수정 사용자 |

### 명명 규칙

- 테이블명과 컬럼명은 `snake_case`를 사용한다.
- 상태 컬럼은 `*_status` 또는 명확한 상태명으로 작성한다.
- 외래키는 `{대상}_id` 형식으로 작성한다.
- JSON 데이터는 용도를 나타내는 이름을 사용하고 무분별한 `metadata` 사용을 피한다.

## 4. 사용자 및 Tenant

### `users`

인증된 개인 사용자를 저장한다. 인증 비밀번호 원문은 애플리케이션 DB에 저장하지 않는다.

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `id` | UUID | PK |
| `email` | varchar | NOT NULL, 소문자 정규화 |
| `email_verified_at` | timestamptz | 인증 완료 시각 |
| `display_name` | varchar | 사용자 표시명 |
| `status` | enum | `PENDING`, `ACTIVE`, `SUSPENDED`, `DELETED` |
| `last_login_at` | timestamptz | 최근 로그인 |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

주요 제약조건:

- 활성 사용자 이메일은 대소문자와 공백을 정규화한 뒤 유일해야 한다.
- 삭제 사용자의 인증 세션은 즉시 폐기한다.

### `tenants`

고객의 데이터 격리 단위다. 초기에는 회원가입 시 개인 Tenant 한 개를 자동 생성한다.

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `id` | UUID | PK |
| `name` | varchar | 조직 또는 개인 작업공간명 |
| `slug` | varchar | 관리 화면 식별용, UNIQUE |
| `status` | enum | `ACTIVE`, `SUSPENDED`, `CLOSING`, `DELETED` |
| `owner_user_id` | UUID | FK → users |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

### `tenant_members`

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `tenant_id` | UUID | FK → tenants |
| `user_id` | UUID | FK → users |
| `role` | enum | `OWNER`, `ADMIN`, `EDITOR`, `VIEWER` |
| `status` | enum | `INVITED`, `ACTIVE`, `REVOKED` |
| `invited_at` | timestamptz | nullable |
| `joined_at` | timestamptz | nullable |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |

기본키 또는 유일키: `(tenant_id, user_id)`

## 5. 사이트 핵심 데이터

### `sites`

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | FK → tenants, NOT NULL |
| `name` | varchar | 관리 화면 사이트명 |
| `slug` | varchar | 기본 서브도메인 |
| `site_type` | enum | `CORPORATE`, `LOCAL_STORE`, `PERSONAL_BRAND` |
| `status` | enum | 사이트 상태 참조 |
| `template_version_id` | UUID | FK → template_versions |
| `draft_revision` | bigint | Draft 변경 번호 |
| `published_version_id` | UUID | nullable, FK → site_versions |
| `last_published_at` | timestamptz | nullable |
| `created_by` | UUID | FK → users |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

주요 제약조건:

- 활성 사이트의 `slug`는 전체 서비스에서 유일해야 한다.
- `slug`는 소문자 영문, 숫자, 하이픈만 허용한다.
- 예약어 `www`, `app`, `api`, `admin`, `preview`, `support`는 사용할 수 없다.
- `published_version_id`는 같은 `site_id`에 속한 버전이어야 한다.

### `site_settings`

사이트의 스타일, SEO, 연락처 및 렌더링 설정을 저장한다.

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `site_id` | UUID | PK, FK → sites |
| `tenant_id` | UUID | 데이터 격리 |
| `brand_name` | varchar | 브랜드 표시명 |
| `logo_asset_id` | UUID | nullable, FK → assets |
| `primary_color` | varchar | 허용 형식 검증 |
| `secondary_color` | varchar | nullable |
| `font_key` | varchar | 승인된 폰트 Registry key |
| `contact_email` | varchar | nullable |
| `contact_phone` | varchar | nullable |
| `default_locale` | varchar | 기본 `ko-KR` |
| `seo_defaults` | jsonb | 기본 title·description 규칙 |
| `social_links` | jsonb | 허용된 서비스와 URL |
| `updated_at` | timestamptz | NOT NULL |

### `business_profiles`

AI 기획과 콘텐츠 생성에 사용하는 원본 사업정보를 저장한다.

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `site_id` | UUID | PK, FK → sites |
| `tenant_id` | UUID | 데이터 격리 |
| `industry` | varchar | 업종 |
| `business_summary` | text | 사업 소개 |
| `target_customers` | text | 주요 고객 |
| `customer_problem` | text | 고객 문제 |
| `core_services` | jsonb | 서비스 목록 |
| `differentiators` | jsonb | 차별점 |
| `desired_actions` | jsonb | 문의·전화 등 CTA |
| `address` | jsonb | 구조화 주소 |
| `business_hours` | jsonb | 운영시간 |
| `generation_notes` | text | 생성 참고사항 |
| `completed_at` | timestamptz | 입력 완료 시각 |
| `updated_at` | timestamptz | NOT NULL |

## 6. 페이지와 섹션

### `pages`

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `page_type` | enum | `HOME`, `ABOUT`, `SERVICES`, `CONTACT`, `PRIVACY`, `CUSTOM` |
| `title` | varchar | 화면 제목 |
| `slug` | varchar | 사이트 내부 URL |
| `navigation_label` | varchar | 메뉴 표시명 |
| `navigation_order` | integer | 메뉴 순서 |
| `is_navigation_visible` | boolean | 메뉴 표시 여부 |
| `is_home` | boolean | 홈페이지 여부 |
| `seo_title` | varchar | nullable |
| `seo_description` | varchar | nullable |
| `status` | enum | `DRAFT`, `ACTIVE`, `HIDDEN` |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

주요 유일키:

- 활성 페이지 기준 `(site_id, slug)` UNIQUE
- 사이트당 `is_home = true`는 최대 한 개

### `sections`

| 컬럼 | 형식 | 제약·설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | NOT NULL |
| `page_id` | UUID | FK → pages |
| `section_type` | varchar | 승인된 Section Registry key |
| `sort_order` | integer | 페이지 내 순서 |
| `content` | jsonb | 타입별 Schema를 통과한 콘텐츠 |
| `style_overrides` | jsonb | 허용된 디자인 토큰만 저장 |
| `is_visible` | boolean | 표시 여부 |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

검증 규칙:

- `section_type`별 JSON Schema를 애플리케이션에서 검증한다.
- 임의 스크립트, 이벤트 핸들러 및 실행 가능한 코드를 저장하지 않는다.
- `page_id`, `site_id`, `tenant_id` 관계가 서로 일치해야 한다.

## 7. 템플릿

### `templates`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `key` | varchar | UNIQUE Registry key |
| `name` | varchar | 표시명 |
| `category` | varchar | 대상 업종·용도 |
| `status` | enum | `DRAFT`, `ACTIVE`, `RETIRED` |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |

### `template_versions`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `template_id` | UUID | FK → templates |
| `version` | integer | 템플릿 버전 번호 |
| `schema_version` | integer | 콘텐츠 Schema 버전 |
| `layout_definition` | jsonb | 페이지·섹션 기본 구성 |
| `default_tokens` | jsonb | 색상·간격·폰트 기본값 |
| `status` | enum | `DRAFT`, `PUBLISHED`, `RETIRED` |
| `published_at` | timestamptz | nullable |
| `created_at` | timestamptz | NOT NULL |

유일키: `(template_id, version)`

기존 사이트가 사용 중인 템플릿 버전은 삭제하지 않는다.

## 8. 이미지 및 파일

### `assets`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `asset_type` | enum | `IMAGE`, `LOGO`, `DOCUMENT` |
| `storage_key` | varchar | Object Storage 내부 경로 |
| `original_filename` | varchar | 표시용 원본명 |
| `mime_type` | varchar | 검증된 MIME |
| `size_bytes` | bigint | 파일 크기 |
| `width` | integer | 이미지 너비, nullable |
| `height` | integer | 이미지 높이, nullable |
| `checksum` | varchar | 중복·무결성 확인 |
| `processing_status` | enum | `UPLOADING`, `PROCESSING`, `READY`, `FAILED` |
| `alt_text` | varchar | 접근성 대체 문구 |
| `created_at` | timestamptz | NOT NULL |
| `deleted_at` | timestamptz | nullable |

## 9. 버전과 Preview

### `site_versions`

게시 가능한 사이트 전체 스냅샷을 저장한다.

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `version_number` | bigint | 사이트별 증가 번호 |
| `source_revision` | bigint | 생성 당시 Draft revision |
| `template_version_id` | UUID | 사용한 템플릿 버전 |
| `snapshot` | jsonb | 렌더링에 필요한 전체 불변 데이터 |
| `snapshot_checksum` | varchar | 무결성 확인 |
| `status` | enum | `CREATED`, `QA_RUNNING`, `QA_FAILED`, `READY`, `PUBLISHED`, `SUPERSEDED` |
| `qa_result` | jsonb | 검사 결과 요약 |
| `created_by` | UUID | 사용자 또는 시스템 |
| `created_at` | timestamptz | NOT NULL |
| `published_at` | timestamptz | nullable |

주요 제약조건:

- `(site_id, version_number)` UNIQUE
- 생성 완료 이후 `snapshot`은 수정하지 않는다.
- 같은 `snapshot_checksum`으로 불필요한 중복 버전을 만들지 않는다.

### `preview_tokens`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `version_id` | UUID | FK → site_versions |
| `token_hash` | varchar | 원문 대신 해시 저장 |
| `expires_at` | timestamptz | 만료 시각 |
| `revoked_at` | timestamptz | 폐기 시각 |
| `access_mode` | enum | `TOKEN`, `AUTHENTICATED`, `PASSWORD` |
| `created_at` | timestamptz | NOT NULL |

토큰 원문은 생성 응답에서 한 번만 제공하고 DB에는 해시만 저장한다.

## 10. 작업과 재시도

### `jobs`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `job_type` | enum | `GENERATE`, `CREATE_PREVIEW`, `QA`, `PUBLISH`, `ROLLBACK`, `ASSET_PROCESS` |
| `status` | enum | 작업 상태 참조 |
| `idempotency_key` | varchar | 중복 실행 방지 |
| `input` | jsonb | 민감정보 제외 입력 스냅샷 |
| `result` | jsonb | 완료 결과 |
| `error_code` | varchar | nullable |
| `error_summary` | text | 사용자 노출 가능한 요약 |
| `retry_count` | integer | 기본 0 |
| `max_retries` | integer | 작업별 제한 |
| `next_retry_at` | timestamptz | nullable |
| `started_at` | timestamptz | nullable |
| `completed_at` | timestamptz | nullable |
| `created_by` | UUID | 사용자 또는 시스템 |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |

유일키: `(tenant_id, job_type, idempotency_key)`

### `job_attempts`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `job_id` | UUID | FK → jobs |
| `attempt_number` | integer | 시도 번호 |
| `status` | enum | `RUNNING`, `SUCCEEDED`, `FAILED` |
| `error_class` | enum | `TRANSIENT`, `PERMANENT`, nullable |
| `error_code` | varchar | nullable |
| `error_detail` | text | 민감정보 제거 로그 |
| `started_at` | timestamptz | NOT NULL |
| `completed_at` | timestamptz | nullable |

유일키: `(job_id, attempt_number)`

## 11. 배포

### `deployments`

MVP에서 공개는 데이터 버전 전환이지만, 운영 추적을 위해 배포 이벤트를 저장한다.

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `version_id` | UUID | FK → site_versions |
| `job_id` | UUID | FK → jobs |
| `deployment_type` | enum | `PUBLISH`, `ROLLBACK` |
| `status` | enum | `PENDING`, `CHECKING`, `ACTIVE`, `FAILED`, `ROLLED_BACK` |
| `previous_version_id` | UUID | nullable |
| `public_url` | varchar | 공개 URL |
| `healthcheck_result` | jsonb | 게시 후 검사 결과 |
| `activated_at` | timestamptz | nullable |
| `created_at` | timestamptz | NOT NULL |

## 12. Custom Domain — 후속 확장

### `site_domains`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | FK → sites |
| `hostname` | varchar | 소문자·punycode 정규화 |
| `domain_type` | enum | `DEFAULT_SUBDOMAIN`, `CUSTOM` |
| `status` | enum | 도메인 상태 참조 |
| `is_primary` | boolean | 대표 도메인 여부 |
| `verification_method` | enum | `TXT`, `CNAME`, `NAMESERVER` |
| `verification_data` | jsonb | 필요한 DNS 정보 |
| `verified_at` | timestamptz | nullable |
| `ssl_status` | enum | `PENDING`, `ACTIVE`, `FAILED` |
| `last_checked_at` | timestamptz | nullable |
| `created_at` | timestamptz | NOT NULL |
| `updated_at` | timestamptz | NOT NULL |

주요 제약조건:

- 활성 `hostname`은 전체 시스템에서 UNIQUE
- 사이트당 대표 도메인은 최대 한 개

## 13. 감사 로그

### `audit_logs`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | UUID | PK |
| `tenant_id` | UUID | NOT NULL |
| `site_id` | UUID | nullable |
| `actor_type` | enum | `USER`, `ADMIN`, `SYSTEM` |
| `actor_id` | UUID | nullable |
| `action` | varchar | 예: `site.published` |
| `target_type` | varchar | 변경 대상 종류 |
| `target_id` | UUID | nullable |
| `before_data` | jsonb | 민감정보 제외 변경 전 값 |
| `after_data` | jsonb | 민감정보 제외 변경 후 값 |
| `request_id` | varchar | 요청 추적 ID |
| `ip_hash` | varchar | 필요 시 비식별 처리 |
| `created_at` | timestamptz | NOT NULL |

감사 로그는 일반 사용자 기능에서 수정·삭제할 수 없게 한다.

## 14. 사이트 상태 전이

### 상태 정의

| 상태 | 의미 | 허용되는 주요 행동 |
|---|---|---|
| `DRAFT` | 정보 입력·편집 중 | 입력, 수정, 생성 요청 |
| `GENERATING` | 사이트 생성 중 | 상태 조회, 취소 요청 |
| `PREVIEW_READY` | Preview 확인 가능 | 수정, QA, 새 버전 생성 |
| `QA_RUNNING` | 자동 검사 중 | 상태 조회 |
| `QA_FAILED` | 게시 조건 미충족 | 수정, 재검사 |
| `READY_TO_PUBLISH` | QA 통과 | 게시 승인 |
| `PUBLISHING` | 운영 전환 중 | 상태 조회 |
| `PUBLISHED` | 운영 중 | 수정, 새 Preview, 롤백 |
| `PUBLISH_FAILED` | 새 버전 게시 실패 | 재시도, 기존 버전 유지 |
| `SUSPENDED` | 운영 일시 중단 | 관리자 복구 |
| `ARCHIVED` | 사용자 삭제 대기 | 복구, 보존기간 후 삭제 |

### 전이 규칙

```text
DRAFT
→ GENERATING
→ PREVIEW_READY
→ QA_RUNNING
├─ QA_FAILED → DRAFT 또는 QA_RUNNING
└─ READY_TO_PUBLISH
   → PUBLISHING
   ├─ PUBLISHED
   └─ PUBLISH_FAILED → READY_TO_PUBLISH

PUBLISHED
→ DRAFT 편집
→ 새 PREVIEW_READY
→ 새 버전 PUBLISHING
→ PUBLISHED

모든 활성 상태
→ SUSPENDED
→ 이전 활성 상태로 복구

DRAFT 또는 PUBLISHED
→ ARCHIVED
→ 복구 또는 영구 삭제
```

### 상태 변경 안전 규칙

- 요청이 기대하는 현재 상태를 조건에 포함해 원자적으로 갱신한다.
- 상태 변경과 감사 로그 기록은 가능한 한 같은 트랜잭션에서 처리한다.
- `PUBLISHING → PUBLISHED` 전환 시에만 `published_version_id`를 변경한다.
- 게시 후 헬스체크 실패 시 기존 `published_version_id`를 유지하거나 즉시 복구한다.
- 오래 멈춘 실행 상태는 감시 작업이 찾아 재시도 또는 실패 처리한다.

## 15. 작업 상태 전이

```text
QUEUED
→ RUNNING
├─ SUCCEEDED
├─ RETRY_WAIT → QUEUED
├─ FAILED
└─ CANCELLED
```

### 오류 분류

| 분류 | 예시 | 처리 |
|---|---|---|
| 일시적 오류 | 429, 외부 API 5xx, 네트워크 타임아웃 | 지수 백오프 재시도 |
| 영구 오류 | 잘못된 입력, 권한 없음, 존재하지 않는 템플릿 | 즉시 실패 |
| 검수 오류 | 링크 누락, 필수 SEO 누락 | QA 실패 후 사용자 수정 |
| 시스템 오류 | Schema 불일치, 데이터 관계 손상 | 관리자 알림·자동 게시 차단 |

## 16. 주요 인덱스

| 테이블 | 인덱스 | 목적 |
|---|---|---|
| `tenant_members` | `(user_id, status)` | 로그인 후 Tenant 조회 |
| `sites` | `(tenant_id, status)` | 고객 사이트 목록 |
| `sites` | `(slug)` partial unique | 기본 서브도메인 탐색 |
| `pages` | `(tenant_id, site_id, slug)` | 페이지 라우팅 |
| `sections` | `(page_id, sort_order)` | 섹션 순서 렌더링 |
| `site_versions` | `(site_id, version_number desc)` | 최신 버전 조회 |
| `preview_tokens` | `(token_hash)` unique | Preview 인증 |
| `jobs` | `(status, next_retry_at)` | 실행·재시도 대상 조회 |
| `jobs` | `(tenant_id, site_id, created_at desc)` | 작업 이력 |
| `site_domains` | `(hostname)` unique | Host 기반 사이트 조회 |
| `audit_logs` | `(tenant_id, created_at desc)` | 변경 이력 조회 |

논리 삭제를 사용하는 유일 인덱스는 `deleted_at IS NULL` 조건을 고려한다.

## 17. 데이터 격리 검증

반드시 자동 테스트해야 하는 항목:

- Tenant A 사용자가 Tenant B의 `site_id`를 조회할 수 없다.
- Tenant A 사용자가 Tenant B의 페이지·이미지·버전을 수정할 수 없다.
- Preview 토큰이 다른 버전이나 사이트로 교체되지 않는다.
- 삭제된 멤버의 세션으로 Tenant 데이터에 접근할 수 없다.
- 관리자 접근은 사유와 대상이 감사 로그에 남는다.
- Object Storage의 다른 Tenant 경로에 업로드·다운로드할 수 없다.
- 공개 요청은 오직 Published 버전만 반환한다.

## 18. 보존 및 삭제 정책 초안

| 데이터 | 기본 보존 정책 |
|---|---|
| 활성 사이트 데이터 | 서비스 이용 중 보존 |
| 삭제 요청 사이트 | 30일 복구 대기 후 영구 삭제 후보 |
| 사이트 버전 | 최근 버전 수 또는 기간 기준 보존 |
| 작업 상세 로그 | 운영 분석 기간 후 요약만 보존 |
| 감사 로그 | 정책상 정한 기간 동안 변경 불가 보존 |
| 업로드 파일 | 참조 제거 후 유예기간을 거쳐 삭제 |
| 회원 개인정보 | 법적·계약상 필요 기간 후 삭제 또는 비식별화 |

실제 기간은 이용약관·개인정보처리방침·결제 정책 확정 시 재검토한다.

## 19. 마이그레이션 원칙

- 모든 Schema 변경은 마이그레이션 파일로 관리한다.
- 파괴적 변경은 추가 → 데이터 이전 → 읽기 전환 → 기존 컬럼 제거 순서로 진행한다.
- 운영 배포와 DB 마이그레이션의 호환 구간을 확보한다.
- 긴 잠금이 필요한 변경은 트래픽이 낮은 시간에 수행한다.
- 마이그레이션 전 백업과 복구 절차를 확인한다.
- 실패 시 애플리케이션과 DB 양쪽의 롤백 가능성을 검토한다.

## 20. 4일차 완료 판정

- [x] 전체 ERD와 데이터 경계 정의
- [x] 사용자·Tenant·사이트 Schema 정의
- [x] 페이지·섹션·템플릿 Schema 정의
- [x] Asset·Version·Preview Schema 정의
- [x] Job·Deployment·Audit Schema 정의
- [x] 사이트와 작업 상태 전이 정의
- [x] 제약조건·인덱스·격리 테스트 정의
- [x] 보존·삭제·마이그레이션 원칙 정의
- [ ] ORM과 인증·DB 공급자 확정
- [ ] 서비스 책임자의 DB 설계 승인

4일차 설계가 승인되면 5일차 저장소·개발환경·CI 구성으로 진행한다.

