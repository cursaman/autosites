# 2일차 — 사용자 흐름 설계

## 1. 설계 목표

신규 사용자가 회원가입 후 사업정보를 입력하고 홈페이지를 생성하여 Preview에서 수정한 뒤 기본 URL로 공개하는 전 과정을 정의한다.

### 핵심 원칙

- 사용자가 현재 단계와 다음 행동을 항상 알 수 있어야 한다.
- 입력 내용은 단계마다 자동 저장한다.
- 실패한 단계부터 안전하게 다시 시작할 수 있어야 한다.
- Preview와 운영 사이트를 분리한다.
- 자동 QA를 통과하지 않은 변경은 게시하지 않는다.
- 게시 실패 시 기존 운영 버전을 유지한다.

## 2. 사용자 유형

| 사용자 | 설명 | 주요 권한 |
|---|---|---|
| 방문자 | 가입하지 않은 사용자 | 소개·요금제·샘플 사이트 열람 |
| 고객 | 홈페이지를 만드는 사용자 | 본인 사이트 생성·수정·게시 |
| 관리자 | AutoSites 운영 담당자 | 사용자·사이트·실패 작업·배포 관리 |
| 시스템 | 자동화 작업 실행 주체 | 콘텐츠 생성·검사·게시·복구 |

## 3. 전체 핵심 흐름

```text
서비스 방문
→ 회원가입 또는 로그인
→ 새 홈페이지 만들기
→ 홈페이지 유형 선택
→ 사업정보 입력
→ 템플릿 선택
→ 홈페이지 자동 생성
→ Preview 확인
→ 콘텐츠 수정
→ 자동 QA
→ 게시 승인
→ 기본 서브도메인 공개
→ 운영 상태 확인
```

## 4. 화면 및 경로 목록

| 화면 ID | 화면명 | 권한 | 권장 경로 | 핵심 행동 |
|---|---|---|---|---|
| PG-001 | 서비스 소개 | 전체 | `/` | 시작하기 |
| PG-002 | 회원가입 | 비회원 | `/signup` | 계정 생성 |
| PG-003 | 로그인 | 비회원 | `/login` | 인증 |
| PG-010 | 대시보드 | 고객 | `/dashboard` | 사이트 목록 확인 |
| PG-011 | 홈페이지 유형 | 고객 | `/sites/new/type` | 유형 선택 |
| PG-012 | 사업정보 입력 | 고객 | `/sites/new/business` | 생성 정보 입력 |
| PG-013 | 템플릿 선택 | 고객 | `/sites/new/template` | 디자인 선택 |
| PG-014 | 생성 진행 | 고객 | `/sites/new/generating` | 생성 상태 확인 |
| PG-020 | 사이트 관리 | 고객 | `/sites/{siteId}` | 상태·작업 확인 |
| PG-021 | 콘텐츠 편집 | 고객 | `/sites/{siteId}/editor` | 텍스트·이미지 수정 |
| PG-022 | Preview | 고객 | `/preview/{previewToken}` | 생성 결과 확인 |
| PG-023 | 게시 확인 | 고객 | `/sites/{siteId}/publish` | 검사 결과·게시 승인 |
| PG-024 | 배포 이력 | 고객 | `/sites/{siteId}/versions` | 버전 확인·복구 |
| PG-030 | 운영 사이트 | 전체 | `https://{slug}.{serviceDomain}` | 공개 홈페이지 이용 |
| PG-900 | 관리자 사이트 관리 | 관리자 | `/admin/sites` | 실패·상태·재실행 관리 |

## 5. Flow A — 회원가입과 로그인

### 정상 흐름

```text
PG-001 서비스 소개
→ ‘무료로 시작하기’ 클릭
→ PG-002 이메일·비밀번호 입력
→ 이용약관·개인정보 수집 동의
→ 이메일 인증
→ 계정 생성
→ PG-010 대시보드 이동
```

### 예외 흐름

| 조건 | 시스템 처리 | 사용자 안내 |
|---|---|---|
| 이미 가입된 이메일 | 계정 생성 중단 | 로그인 또는 비밀번호 재설정 안내 |
| 이메일 형식 오류 | 제출 차단 | 올바른 이메일 입력 요청 |
| 약한 비밀번호 | 제출 차단 | 비밀번호 조건 표시 |
| 인증메일 만료 | 계정 미인증 유지 | 인증메일 재발송 제공 |
| 로그인 연속 실패 | 일시 제한 | 재시도 시간과 비밀번호 재설정 제공 |

### 완료 조건

- 인증된 고객만 대시보드에 접근한다.
- 로그인 전 요청한 경로가 있으면 로그인 후 해당 경로로 돌아간다.
- 다른 고객의 `siteId`를 입력해도 접근할 수 없다.

## 6. Flow B — 새 홈페이지 만들기

### 정상 흐름

```text
PG-010 대시보드
→ ‘새 홈페이지 만들기’ 클릭
→ 임시 사이트 생성
→ PG-011 홈페이지 유형 선택
→ 선택값 자동 저장
→ PG-012 사업정보 입력
→ 단계별 자동 저장
→ PG-013 템플릿 선택
→ 생성 요청 확인
```

### 수집 정보

| 구분 | 필수 항목 | 선택 항목 |
|---|---|---|
| 기본 정보 | 회사·브랜드명, 업종 | 설립연도, 대표자 |
| 서비스 | 핵심 서비스명, 설명 | 가격, 상세 특징 |
| 고객 | 주요 고객, 원하는 행동 | 고객 고민·차별점 |
| 연락처 | 전화 또는 이메일 | 주소, 운영시간, 지도 |
| 디자인 | 템플릿, 대표 색상 | 로고, 보조 색상 |
| 페이지 | 기본 5개 페이지 | 추가 섹션 선택 |

### 중단·복귀 흐름

```text
입력 중 이탈
→ 마지막 완료 단계와 입력값 저장
→ 대시보드에 ‘작성 중’ 표시
→ ‘계속 작성’ 클릭
→ 마지막 단계부터 재개
```

### 예외 흐름

| 조건 | 처리 |
|---|---|
| 필수정보 누락 | 다음 단계 이동 차단 및 해당 필드 표시 |
| 저장 실패 | 로컬 임시 보관 후 재시도 안내 |
| 중복 slug | 사용 가능한 대체 주소 제안 |
| 이미지 형식·용량 오류 | 허용 형식과 최대 용량 안내 |
| 세션 만료 | 재로그인 후 작성 상태 복원 |

## 7. Flow C — 홈페이지 자동 생성

### 상태 흐름

```text
DRAFT
→ GENERATION_QUEUED
→ GENERATING_CONTENT
→ BUILDING_PAGES
→ VALIDATING
→ PREVIEW_READY
```

### 정상 흐름

```text
생성 요청
→ 입력 데이터 스냅샷 저장
→ 템플릿 설정 불러오기
→ 페이지별 콘텐츠 생성
→ 메뉴와 섹션 조립
→ 이미지·디자인 토큰 적용
→ 데이터 유효성 검사
→ Preview 버전 저장
→ PG-022 이동
```

### 진행 화면 표시

- 현재 단계
- 전체 진행률
- 시작 시간
- 완료된 작업
- 실패 시 오류 요약과 다시 시도 버튼
- 화면을 닫아도 작업이 계속된다는 안내

### 생성 실패 흐름

```text
일시적 오류
→ 자동 재시도 최대 3회
→ 성공 시 다음 단계 진행

반복 실패
→ GENERATION_FAILED
→ 입력 데이터와 성공한 단계 보존
→ 고객에게 재시도 제공
→ 관리자 오류 알림
```

### 완료 조건

- 동일한 생성 요청을 다시 받아도 사이트가 중복 생성되지 않는다.
- 실패 시 기존 입력값과 이전 Preview를 삭제하지 않는다.
- 생성 결과에 기본 5개 페이지와 메뉴가 존재한다.

## 8. Flow D — Preview 확인과 수정

### 정상 흐름

```text
PREVIEW_READY
→ PG-022 Preview 확인
→ 모바일·태블릿·PC 전환
→ ‘수정하기’ 클릭
→ PG-021 콘텐츠 편집
→ 변경사항 자동 저장
→ 새 Preview 버전 생성
→ 변경 결과 확인
```

### 수정 가능한 항목

- 페이지 제목과 본문
- 서비스 이름과 설명
- CTA 문구와 연결 대상
- 로고와 이미지
- 대표·보조 색상
- 연락처와 주소
- 섹션 표시 여부와 순서

### 수정 제한 항목

- 시스템 공통 코드
- 인증·권한 설정
- 고객 소유권 정보
- 다른 고객 데이터
- 보안·배포 환경변수

### 이탈 방지

- 저장되지 않은 변경이 있을 때 이동 경고
- 자동 저장 상태 표시
- 마지막 저장 시각 표시
- 이전 버전으로 돌아가기 제공

## 9. Flow E — 자동 QA와 게시

### 정상 흐름

```text
‘게시하기’ 클릭
→ 게시 대상 Preview 버전 고정
→ QA_QUEUED
→ 링크·이미지·SEO·필수정보 검사
→ QA_PASSED
→ 게시 확인 화면
→ 고객 최종 승인
→ PUBLISHING
→ 기본 서브도메인 연결
→ 헬스체크
→ PUBLISHED
```

### 자동 QA 항목

| 검사 | 통과 조건 |
|---|---|
| 필수 페이지 | 기본 5개 페이지 존재 |
| 내부 링크 | 모두 정상 경로로 연결 |
| 이미지 | 누락·깨짐 없음 |
| 연락처 | 전화 또는 이메일 존재 |
| SEO | title·description·canonical 존재 |
| 반응형 | 지정 해상도에서 주요 UI 사용 가능 |
| 보안 | Preview 토큰·관리 경로 미노출 |
| 응답 | 주요 URL이 정상 상태 반환 |

### QA 실패 흐름

```text
QA_FAILED
→ 실패 항목과 위치 표시
→ 자동 수정 가능한 항목은 수정 제안
→ 고객이 편집 화면에서 수정
→ 실패한 검사부터 재실행
```

### 게시 실패 흐름

```text
PUBLISH_FAILED
→ 기존 운영 버전 유지
→ 새 버전 활성화 금지
→ 일시적 오류 자동 재시도
→ 반복 실패 시 관리자 알림
→ 고객에게 현재 운영 상태 안내
```

## 10. Flow F — 공개 후 운영과 복구

### 정상 운영

```text
PG-020 사이트 관리
→ 운영 URL·현재 버전·최근 배포 확인
→ 콘텐츠 수정
→ 새 Preview 생성
→ QA
→ 승인
→ 새 운영 버전 게시
```

### 롤백 흐름

```text
운영 문제 발견
→ PG-024 배포 이력
→ 정상 버전 선택
→ 복구 영향 확인
→ 롤백 승인
→ 선택 버전을 운영 버전으로 전환
→ 헬스체크
→ 복구 완료 기록
```

### 삭제 흐름

```text
사이트 삭제 요청
→ 사이트명 재입력 확인
→ 즉시 비공개
→ 복구 대기 상태
→ 보존기간 내 복구 가능
→ 보존기간 후 영구 삭제
```

## 11. 관리자 대응 흐름

```text
시스템 오류 감지
→ 관리자 알림
→ PG-900에서 사이트·작업 상태 확인
→ 오류 단계와 로그 확인
→ 실패 단계 재실행 또는 이전 버전 복구
→ 처리 결과 기록
→ 필요 시 고객에게 안내
```

관리자가 제공받아야 하는 정보:

- 사용자·사이트 ID
- 작업 ID와 멱등성 키
- 실패 단계와 오류 코드
- 재시도 횟수
- 마지막 정상 버전
- 생성·QA·게시 시작 및 종료 시각
- 고객에게 표시된 오류 메시지

## 12. 핵심 상태 전이

```text
DRAFT
├─ GENERATING
│  ├─ PREVIEW_READY
│  └─ GENERATION_FAILED → GENERATING
├─ QA_RUNNING
│  ├─ QA_PASSED
│  └─ QA_FAILED → DRAFT
├─ PUBLISHING
│  ├─ PUBLISHED
│  └─ PUBLISH_FAILED → 이전 PUBLISHED 유지
└─ ARCHIVED → 복구 또는 영구 삭제
```

## 13. 주요 이벤트

| 이벤트 | 발생 시점 | 활용 목적 |
|---|---|---|
| `signup_completed` | 회원가입 완료 | 가입 전환 측정 |
| `site_draft_created` | 새 사이트 시작 | 생성 시작 측정 |
| `business_info_completed` | 사업정보 완료 | 입력 이탈 분석 |
| `generation_started` | 생성 요청 | 생성량 측정 |
| `preview_ready` | Preview 완료 | 생성 성공률 측정 |
| `edit_saved` | 수정 저장 | 편집 사용 분석 |
| `qa_failed` | 자동 검사 실패 | 품질 문제 분석 |
| `publish_started` | 게시 시작 | 배포 전환 측정 |
| `site_published` | 공개 성공 | 핵심 전환 측정 |
| `rollback_completed` | 복구 완료 | 운영 안정성 분석 |

## 14. 핵심 성과 퍼널

```text
회원가입 완료
→ 사이트 생성 시작
→ 사업정보 입력 완료
→ Preview 생성 완료
→ 1회 이상 수정
→ 게시 성공
```

각 단계별 완료율과 소요시간을 기록한다. 가장 이탈률이 높은 단계를 우선 개선한다.

## 15. 2일차 완료 판정

- [x] 사용자 유형과 권한 정의
- [x] 전체 정상 흐름 정의
- [x] 주요 화면과 경로 정의
- [x] 회원가입·입력·생성·Preview·게시 흐름 정의
- [x] 오류·중단·재시도 흐름 정의
- [x] 롤백과 삭제 흐름 정의
- [x] 관리자 대응 흐름 정의
- [x] 상태 전이와 분석 이벤트 정의
- [ ] 서비스 책임자의 User Flow 승인

2일차 문서 검토와 승인이 완료되면 3일차 멀티테넌트 시스템 구조 설계로 진행한다.

