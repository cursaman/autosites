# AutoSites Codex 표준 작업 흐름

## 목적

사용자가 자연어로 홈페이지 변경을 요청하면 Codex가 현재 디자인을 보존하면서 수정하고, 자동 검사를 통과한 결과만 GitHub와 대표 Vercel 운영 주소에 반영한다.

## 요청 해석 순서

1. **목표** — 방문자가 무엇을 이해하거나 행동해야 하는가?
2. **범위** — 어느 페이지·섹션·문구·이미지·기능을 바꾸는가?
3. **보존 조건** — 유지해야 할 색상, 구성, 링크와 기존 기능은 무엇인가?
4. **완료 조건** — 모바일, 링크, 문구, 배포 중 무엇을 확인해야 하는가?

정보가 없어도 기존 디자인과 콘텐츠에서 안전하게 추론할 수 있으면 바로 진행한다. 결과가 크게 달라지는 선택이나 삭제·결제·도메인 변경처럼 외부 상태에 영향이 큰 작업만 사용자에게 확인한다.

## 작업 유형별 기준

### 문구와 콘텐츠

- `src/content/site-content.ts`를 우선 수정한다.
- 임시 문구 대신 실제 공개 가능한 한국어를 사용한다.
- 제목, 설명, CTA의 의미가 서로 이어지는지 확인한다.
- 날짜, 가격, 모집 상태와 수업 완료 여부는 확인된 정보만 단정적으로 표시한다.
- 이름, 연락처, 사진과 후기는 공개 동의 범위를 넘겨 사용하지 않는다.

### 페이지별 기준

- `/` — 방문자가 서비스와 핵심 행동을 빠르게 이해하는 메인 페이지
- `/course` — 교육 일정, 진행 기록, 비용, 장소와 모집 방법의 상세 페이지
- `/setup` — Codex·Git·GitHub·Vercel과 선택 기능인 Supabase의 준비 안내
- 메인에 상세 설명을 반복하지 않고 적절한 상세 페이지로 연결한다.

### 디자인

- `src/styles/design-tokens.css`의 색상과 간격을 우선 재사용한다.
- 페이지 전용 스타일은 CSS Module에 작성한다.
- 600px 이하 모바일, 900px 이하 태블릿과 데스크톱을 확인한다.
- 키보드 포커스와 `prefers-reduced-motion`을 유지한다.

### 이미지

- 실제 사진이 필요하면 적절한 이미지 검색을 우선하고 독창적인 전용 비주얼이 필요할 때만 이미지 생성을 사용한다.
- 프로젝트 이미지 파일은 `public/images`에 저장한다.
- `next/image`, 반응형 `sizes`와 구체적인 한국어 대체 텍스트를 적용한다.
- 생성 이미지는 로고, 문구, 워터마크와 사실 오류를 확인한다.

### 기능과 링크

- 요청하지 않은 로그인, 데이터베이스, 결제와 고객용 편집기를 추가하지 않는다.
- CTA 목적지와 내부 `#section` ID를 함께 확인한다.
- 새 외부 링크는 HTTPS를 사용하고 새 창 링크에는 안전한 `rel` 속성을 적용한다.

### 검색과 공유

- 새 공개 페이지에는 고유한 title, description과 canonical을 설정한다.
- 독립적으로 공유되는 페이지는 Open Graph와 X 카드 문구를 페이지 내용에 맞춘다.
- 공개 페이지를 추가하면 `src/app/sitemap.ts`와 자동 검사를 함께 갱신한다.

## 배포 전 필수 검사

```bash
npm run verify
```

검사는 ESLint, TypeScript, 프로덕션 빌드, 필수 문구, 내부 링크, SEO 경로, 이미지 응답과 런타임 오류를 포함한다. 하나라도 실패하면 커밋과 Push를 중단하고 수정 후 전체 검사를 다시 실행한다.

## GitHub와 Vercel 반영

```text
검사 통과
→ 의도한 파일만 Git commit
→ git push origin main
→ autosites-jd3d 배포 READY 대기
→ npm run deployment:status
→ 운영 주소에서 변경 내용 확인
```

대표 운영 정보는 `deployment-target.json`만 기준으로 한다. 중복 프로젝트 `autosites`는 경고 대상이며 사용자 승인 없이 삭제하거나 운영 대상으로 사용하지 않는다.

운영 확인은 GitHub 상태가 성공한 것만으로 끝내지 않는다. 변경한 경로를 실제 운영 주소에서 다시 요청하고 최신 문구, 링크와 이미지 응답을 확인한다.

## 완료 보고 형식

```text
완료된 변경: 핵심 결과
검사: verify 통과
GitHub: 짧은 커밋 SHA
Vercel: READY
운영 주소: https://autosites-jd3d.vercel.app
남은 경고: 없으면 생략
```

## 실패와 롤백

- 빌드 실패: Push하지 않고 원인을 수정한다.
- Vercel 실패: 빌드 로그를 확인하고 새 수정 커밋으로 복구한다.
- 운영 오류: 가장 최근 정상 배포를 확인하고 `git revert <문제 SHA>`를 우선 사용한다.
- 강제 Push와 공유 `main` 이력 재작성은 금지한다.
