# 새기획 07일차 — 자동 검증 구성

## 오늘의 목표

배포 전에 필요한 검사를 한 번의 명령으로 실행하고 하나라도 실패하면 GitHub Push와 운영 배포를 진행하지 않는다.

## 단일 검증 명령

```bash
pnpm verify
```

`npm run verify`로도 동일한 검사를 실행할 수 있다.

## 자동 검사 순서

```text
ESLint
→ TypeScript
→ Next.js 프로덕션 빌드
→ 프로덕션 서버 실행
→ 필수 문구 검사
→ 내부 링크 연결 검사
→ SEO·robots·sitemap·공유 이미지 검사
→ 서버 오류 검사
```

## 구현 내용

- `scripts/verify-site.mjs` 프로덕션 홈페이지 검사기 추가
- 메인 화면 필수 문구 5개 검사
- `#` 내부 링크가 실제 섹션 ID와 연결되는지 검사
- canonical, Open Graph와 X 카드 메타정보 검사
- `robots.txt`, `sitemap.xml`, `og.png` 응답과 콘텐츠 형식 검사
- 서버 출력에서 TypeError, ReferenceError와 Internal Server Error 검사
- 30초 내 서버가 준비되지 않으면 실패 처리
- GitHub Actions도 `pnpm verify` 한 번으로 동일하게 검사

## 배포 차단 규칙

1. Codex는 수정 후 항상 `pnpm verify`를 실행한다.
2. 종료 코드가 0일 때만 커밋하고 GitHub에 Push한다.
3. 실패하면 Push하지 않고 원인을 수정한 뒤 처음부터 다시 검사한다.
4. Vercel에서는 GitHub `main`에 올라온 검증 완료 커밋만 자동 배포한다.

## 완료 기준

- [x] 한 번의 명령으로 모든 검사를 실행한다.
- [x] 필수 문구와 내부 링크를 자동 검사한다.
- [x] 프로덕션 서버와 SEO 경로를 자동 검사한다.
- [x] GitHub Actions에서도 같은 명령을 사용한다.
- [x] 실제 단일 검증 명령 통과
- [x] GitHub Push 및 Vercel 운영 화면 확인

## 다음 단계

8일차에는 대표 GitHub 저장소와 Vercel 프로젝트의 자동 배포 연결, 배포 커밋 일치와 롤백 절차를 정리한다.
