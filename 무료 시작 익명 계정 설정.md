# 무료 시작 — 익명 계정 설정

## 동작 방식

- 방문자는 로그인하지 않고 홈페이지 유형과 사업정보를 입력한다.
- 첫 임시저장 시 Supabase 익명 사용자를 자동 생성한다.
- 익명 사용자도 고유 ID와 인증 세션을 가지므로 기존 RLS가 그대로 적용된다.
- 브라우저 쿠키가 유지되는 동안 내 사이트와 입력 내용을 다시 확인할 수 있다.

## Supabase 설정

Supabase Dashboard에서 다음 설정을 활성화한다.

```text
Authentication → Providers → Anonymous Sign-Ins → Enable
```

## 주의사항

- 로그아웃하거나 브라우저 데이터를 삭제하면 같은 익명 계정으로 돌아올 수 없다.
- 운영 전에는 CAPTCHA 또는 Cloudflare Turnstile을 연결해 자동 가입 남용을 방지한다.
- 익명 계정을 이메일 계정으로 전환하는 기능은 후속 단계에서 구현한다.

## 테스트

1. 시크릿 창에서 메인 페이지를 연다.
2. `무료로 시작하기`를 누른다.
3. 로그인 화면 없이 유형 선택 화면이 열리는지 확인한다.
4. 사업정보를 입력하고 임시저장한다.
5. Supabase Authentication의 Users에서 익명 사용자가 생성됐는지 확인한다.
6. Table Editor의 `sites`, `business_profiles`에 같은 사용자 ID로 데이터가 저장됐는지 확인한다.
