# 3일차 — 멀티테넌트 시스템 구조 설계

## 1. 설계 목표

하나의 코드베이스와 하나의 공통 배포 프로젝트에서 여러 고객의 홈페이지를 생성·수정·Preview·공개할 수 있는 멀티테넌트 구조를 설계한다.

### 핵심 원칙

- 사이트마다 별도 소스코드·GitHub 저장소·배포 프로젝트를 만들지 않는다.
- 고객 사이트는 공통 템플릿과 고객별 데이터 조합으로 렌더링한다.
- 모든 고객 데이터에는 소유자를 식별하는 `tenant_id` 또는 `site_id`를 포함한다.
- 요청마다 사용자 권한과 사이트 소유권을 서버에서 확인한다.
- 초안, Preview, 운영 버전을 분리한다.
- 생성·검사·게시 작업은 재시도 가능한 비동기 작업으로 처리한다.
- 장애가 발생해도 기존 운영 사이트는 계속 제공한다.

## 2. 권장 기술 구성

| 영역 | 권장 구성 | 역할 |
|---|---|---|
| 웹 애플리케이션 | Next.js + TypeScript | 고객 대시보드, 편집기, 공개 사이트, API |
| UI | React 기반 공통 컴포넌트 | 템플릿과 관리 화면 구성 |
| 데이터베이스 | PostgreSQL | 사용자·사이트·페이지·버전·작업 데이터 |
| ORM | 타입 안전 ORM | 스키마·쿼리·마이그레이션 관리 |
| 인증 | 세션 기반 인증 서비스 | 회원가입·로그인·권한 확인 |
| 파일 저장 | S3 호환 Object Storage | 로고·이미지·업로드 파일 |
| 배포 | Vercel | Preview·Production 배포와 도메인 관리 |
| 비동기 작업 | Durable Workflow 또는 Job Queue | AI 생성·QA·게시·재시도 |
| 캐시·제한 | Redis 호환 저장소 | Rate Limit·작업 잠금·단기 캐시 |
| 오류 수집 | 오류 추적 서비스 | 브라우저·서버 오류 확인 |
| 모니터링 | 플랫폼 로그·분석·헬스체크 | 가용성·성능·배포 상태 확인 |

특정 서비스 업체와 라이브러리 버전은 구현 착수 시 확정한다.

## 3. 전체 시스템 구조

```text
사용자 브라우저
├─ AutoSites 관리 도메인
│  ├─ 회원가입·로그인
│  ├─ 사업정보 입력
│  ├─ 템플릿 선택
│  ├─ 콘텐츠 편집
│  └─ Preview·배포 관리
│
├─ Preview 도메인
│  └─ 초안 버전 렌더링
│
└─ 공개 사이트 도메인
   └─ 게시된 버전 렌더링
          ↓
Next.js 애플리케이션
├─ 인증·권한 계층
├─ Tenant·Site Resolver
├─ 관리 API
├─ 공개 사이트 Renderer
├─ 템플릿·컴포넌트 Registry
└─ 작업 실행 API
          ↓
서비스 계층
├─ Site Service
├─ Content Service
├─ Generation Service
├─ Preview Service
├─ Publish Service
├─ Domain Service
└─ Audit Service
          ↓
데이터·외부 시스템
├─ PostgreSQL
├─ Object Storage
├─ Queue·Workflow
├─ AI Provider
└─ Vercel API
```

## 4. 도메인 및 라우팅 구조

### 관리 화면

```text
app.autosites.example
```

고객은 관리 도메인에서 사이트를 생성·수정·게시한다.

### Preview

```text
preview.autosites.example/p/{previewToken}
```

- 예측하기 어려운 만료 가능한 토큰을 사용한다.
- 검색엔진 색인을 차단한다.
- 토큰이 유효한 경우에만 초안 버전을 렌더링한다.
- 필요하면 로그인 또는 암호 보호를 추가한다.

### 기본 공개 도메인

```text
{siteSlug}.autosites.example
```

- 와일드카드 DNS와 SSL을 사용한다.
- Host 헤더에서 `siteSlug`를 추출한다.
- 해당 사이트의 `published_version_id`만 렌더링한다.

### Custom Domain — 후속 단계

```text
www.customer-domain.com
```

- 요청 Host와 `site_domains.hostname`을 비교한다.
- 검증 및 활성 상태인 도메인만 연결한다.
- 일치하는 사이트의 게시 버전을 렌더링한다.

## 5. 요청 처리 흐름

### 관리 요청

```text
요청 수신
→ 세션 확인
→ tenant_id 확인
→ site_id 소유권 확인
→ 권한 확인
→ 서비스 로직 실행
→ 변경 이력 기록
→ 응답
```

### 공개 사이트 요청

```text
요청 Host 확인
→ 기본 서브도메인 또는 Custom Domain 판별
→ Site Resolver로 site_id 조회
→ active 상태 확인
→ published_version_id 조회
→ 캐시된 게시 데이터 조회
→ 템플릿 Renderer 실행
→ HTML 응답
```

### Preview 요청

```text
previewToken 확인
→ 토큰 해시·만료·폐기 여부 확인
→ 대상 site_id·version_id 조회
→ Preview 버전 렌더링
→ noindex 응답
```

## 6. 핵심 데이터 경계

```text
Tenant
└─ Users
└─ Sites
   ├─ Site Settings
   ├─ Pages
   │  └─ Sections
   ├─ Assets
   ├─ Versions
   ├─ Preview Tokens
   ├─ Domains
   ├─ Jobs
   └─ Audit Logs
```

### 핵심 식별자

| 식별자 | 용도 | 규칙 |
|---|---|---|
| `tenant_id` | 고객 조직 데이터 경계 | 모든 고객 소유 레코드에 포함 |
| `user_id` | 사용자 식별 | 인증 세션과 연결 |
| `site_id` | 홈페이지 식별 | URL에 직접 노출 시 권한 재검증 |
| `version_id` | 초안·게시 버전 식별 | 변경 불가능한 스냅샷 권장 |
| `job_id` | 생성·QA·게시 작업 추적 | 로그·재시도와 연결 |
| `idempotency_key` | 중복 실행 방지 | 동일 요청에 동일 결과 보장 |

## 7. 고객 데이터 분리 전략

MVP에서는 하나의 데이터베이스와 공통 스키마를 사용하고 모든 고객 데이터에 `tenant_id`를 저장한다.

```text
공통 Database
├─ tenant A rows
├─ tenant B rows
└─ tenant C rows
```

### 필수 보호 규칙

- 클라이언트가 전달한 `tenant_id`를 신뢰하지 않는다.
- `tenant_id`는 인증 세션에서 서버가 결정한다.
- 모든 조회·수정·삭제 쿼리에 `tenant_id` 조건을 포함한다.
- `site_id`만으로 데이터를 조회하지 않는다.
- 관리자 우회 접근은 별도 권한과 감사 로그가 필요하다.
- Object Storage 경로도 tenant와 site 단위로 분리한다.

권장 저장 경로:

```text
tenants/{tenantId}/sites/{siteId}/assets/{assetId}
```

### 저장소 접근

- 업로드는 서버가 발급한 짧은 만료시간의 서명 URL을 사용한다.
- 파일 확장자뿐 아니라 MIME type과 실제 콘텐츠를 검증한다.
- 원본 파일명 대신 생성된 안전한 식별자를 사용한다.
- 비공개 원본과 공개 최적화 이미지를 구분한다.

## 8. 템플릿 렌더링 구조

사이트마다 코드를 생성하지 않고 구조화된 사이트 데이터를 공통 Renderer가 읽어 화면을 만든다.

```text
Published Site Data
→ Template Registry
→ Page Layout
→ Section Registry
→ Design Tokens
→ Rendered Website
```

### 템플릿 Registry 예시

```text
corporate-v1
local-store-v1
personal-brand-v1
```

### 섹션 Registry 예시

```text
hero
service-list
about
features
gallery
testimonials
faq
contact
map
footer
```

### 구조화된 콘텐츠 원칙

- AI가 실행 가능한 코드를 직접 생성하지 않는다.
- AI 출력은 승인된 JSON Schema를 따라야 한다.
- 등록되지 않은 섹션 타입은 렌더링하지 않는다.
- 허용된 디자인 토큰만 사용한다.
- HTML 입력은 정제하며 임의 스크립트를 차단한다.

## 9. 초안·버전·게시 구조

```text
현재 편집 상태
→ Draft 저장
→ Preview 요청
→ 불변 Version Snapshot 생성
→ QA
→ 고객 승인
→ published_version_id 변경
```

### 버전 정책

- Draft는 계속 수정할 수 있다.
- Preview와 Published 버전은 불변 스냅샷으로 저장한다.
- 게시 시 사이트의 `published_version_id`만 원자적으로 변경한다.
- 게시 실패 시 기존 `published_version_id`를 유지한다.
- 롤백은 과거 정상 버전을 새 운영 버전으로 지정한다.
- 누가 언제 어떤 버전을 게시했는지 감사 로그에 기록한다.

이 구조에서는 코드 재배포 없이 콘텐츠와 디자인 설정을 게시할 수 있다.

## 10. 비동기 작업 구조

오래 걸리거나 외부 서비스에 의존하는 작업은 요청·응답 처리와 분리한다.

### 비동기 처리 대상

- AI 기획과 콘텐츠 생성
- 페이지 조립과 검증
- 이미지 변환·최적화
- 자동 QA
- 게시 후 헬스체크
- 도메인 DNS 검증
- 알림 발송
- 백업과 복구

### 작업 흐름

```text
API에서 작업 요청
→ jobs 레코드 생성
→ Queue 또는 Workflow 시작
→ 단계별 상태·결과 저장
→ 일시적 오류 자동 재시도
→ 완료 또는 실패 확정
→ 사용자 화면에 상태 반영
```

### 작업 상태

```text
QUEUED
→ RUNNING
→ SUCCEEDED

RUNNING
→ RETRY_WAIT
→ RUNNING

RUNNING
→ FAILED
```

### 작업 안전 규칙

- 요청마다 `idempotency_key`를 저장한다.
- 같은 키의 완료 작업이 있으면 다시 실행하지 않는다.
- 외부 API 호출 전후의 상태를 기록한다.
- 4xx 영구 오류와 429·5xx 일시 오류를 구분한다.
- 재시도 횟수와 최대 실행시간을 제한한다.
- 반복 실패 작업은 관리자 처리 대기열로 이동한다.

## 11. 캐시 전략

| 데이터 | 캐시 여부 | 무효화 시점 |
|---|---|---|
| 공개 사이트 버전 | 적극 캐시 | 새 버전 게시·롤백 |
| 템플릿 정의 | 장기 캐시 | 템플릿 배포 |
| Preview 데이터 | 짧은 캐시 또는 미사용 | 편집 저장·새 Preview |
| 관리 화면 데이터 | 사용자 단기 캐시 | 수정·삭제 |
| 권한 정보 | 짧은 캐시 | 역할·멤버 변경 |
| 작업 상태 | 짧은 폴링 캐시 | 상태 변경 |

캐시 키에는 반드시 `site_id`, `version_id`, 언어 등 렌더링 결과에 영향을 주는 값을 포함한다.

## 12. API 경계

### 고객 관리 API

```text
/api/sites
/api/sites/{siteId}
/api/sites/{siteId}/business-info
/api/sites/{siteId}/pages
/api/sites/{siteId}/assets
/api/sites/{siteId}/generate
/api/sites/{siteId}/preview
/api/sites/{siteId}/qa
/api/sites/{siteId}/publish
/api/sites/{siteId}/versions
/api/sites/{siteId}/rollback
```

### 내부 작업 API

```text
/api/internal/jobs/{jobId}
/api/internal/health
/api/internal/webhooks/*
```

내부 API는 공개 API와 인증 방식을 분리하며 서명, 제한된 토큰 또는 서비스 간 인증을 사용한다.

## 13. 보안 경계

| 위험 | 보호 방법 |
|---|---|
| 타 고객 데이터 접근 | 서버 권한 검사·tenant 조건·격리 테스트 |
| ID 직접 변경 공격 | 추측하기 어려운 ID와 소유권 재검증 |
| Preview URL 유출 | 만료 토큰·폐기·선택적 로그인 보호 |
| 악성 HTML·스크립트 | 허용 목록 기반 정제·CSP |
| 파일 업로드 공격 | MIME·용량·악성 파일 검사 |
| AI 출력 공격 | JSON Schema 검증·코드 실행 금지 |
| API 남용 | 사용자·IP·기능별 Rate Limit |
| 관리자 오용 | 최소 권한·MFA·감사 로그 |
| 비밀정보 노출 | 서버 환경변수·비밀 관리 서비스 사용 |

## 14. 장애 격리와 복구

- 특정 고객의 생성 실패가 다른 고객 작업을 막지 않아야 한다.
- 특정 사이트의 잘못된 데이터가 공통 Renderer를 중단시키지 않아야 한다.
- 외부 AI 서비스 장애 시 기존 공개 사이트는 계속 제공한다.
- DB 쓰기 장애 시 공개 사이트는 캐시된 게시 버전을 제공한다.
- 새 버전 오류 시 이전 게시 버전으로 원자적 롤백한다.
- 실패 작업은 고객·사이트·작업 단위로 재실행한다.

## 15. 확장 전략

### 초기 단계

```text
공통 애플리케이션
+ 공통 DB
+ 공통 Object Storage
+ 공통 배포 프로젝트
```

### 성장 단계

- 공개 사이트 읽기 트래픽과 관리 API 분리
- 비동기 작업 Worker 분리
- 읽기 복제본과 캐시 확대
- 고객 등급별 작업 큐 우선순위
- 미디어 전용 처리 서비스 분리

### 엔터프라이즈 단계

- 고객별 DB 또는 Schema 격리 옵션
- 고객별 저장소·프로젝트 생성 옵션
- 전용 도메인·리전·백업 정책
- 소스코드 Export 상품

## 16. 주요 기술 의사결정

| ID | 결정 | 이유 | 상태 |
|---|---|---|---|
| ADR-001 | 초기에는 공통 코드베이스를 사용한다 | 생성·배포·유지보수 단순화 | 확정 |
| ADR-002 | 고객별 사이트를 데이터로 구성한다 | 사이트별 코드 생성 방지 | 확정 |
| ADR-003 | 공통 DB와 tenant_id 격리를 사용한다 | MVP 개발 속도와 운영 효율 | 확정 |
| ADR-004 | 게시 버전을 불변 스냅샷으로 저장한다 | 안전한 Preview·롤백 | 확정 |
| ADR-005 | 공개는 버전 포인터 전환 방식으로 처리한다 | 무중단 게시와 즉시 복구 | 확정 |
| ADR-006 | 장기 작업은 비동기로 실행한다 | 타임아웃·재시도·복구 대응 | 확정 |
| ADR-007 | AI는 코드를 생성·실행하지 않는다 | 보안과 품질 안정성 | 확정 |
| ADR-008 | 구체적인 인증·DB·스토리지 공급자 | 비용과 운영 조건 비교 필요 | 미정 |

## 17. 3일차 완료 판정

- [x] 공통 멀티테넌트 구조 정의
- [x] 관리·Preview·공개 도메인 분리
- [x] 고객 데이터 격리 규칙 정의
- [x] 템플릿과 구조화 콘텐츠 방식 정의
- [x] Draft·Version·Published 구조 정의
- [x] 비동기 작업과 재시도 구조 정의
- [x] 캐시·보안·장애 복구 원칙 정의
- [x] 성장 단계 확장 전략 정의
- [ ] 서비스 책임자의 시스템 구조 승인

3일차 구조가 승인되면 4일차 DB 스키마와 상태 전이 상세 설계로 진행한다.

