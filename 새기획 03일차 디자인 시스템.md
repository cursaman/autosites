# 새 기획 3일차 — 디자인 시스템

## 1. 디자인 방향

### 콘셉트

`Warm Technical Editorial`

Codex와 GitHub·Vercel이라는 기술적인 내용을 다루지만 개발 도구처럼 차갑게 보이지 않도록 따뜻한 종이색 배경과 짙은 녹색을 사용한다. 큰 제목, 명확한 번호와 단계별 카드로 비개발자도 작업 흐름을 빠르게 이해하게 한다.

### 디자인 원칙

1. 한 화면에는 하나의 핵심 메시지만 강조한다.
2. 장식보다 작업 순서와 결과를 명확히 보여준다.
3. 코드와 요청 예시는 일반 설명과 시각적으로 구분한다.
4. 모바일에서도 단계와 CTA가 끊기지 않게 한다.
5. 애니메이션은 의미 있는 상태 변화에만 사용한다.

## 2. 색상

| 역할 | 값 | 용도 |
|---|---|---|
| Canvas | `#F4F1E9` | 전체 배경 |
| Surface | `#FFFDF8` | 카드와 강조 영역 |
| Surface Strong | `#E8EEE8` | 선택·보조 영역 |
| Ink | `#142019` | 제목과 본문 핵심 텍스트 |
| Ink Soft | `#536059` | 설명과 보조 텍스트 |
| Line | `#CFD5CE` | 구분선과 카드 테두리 |
| Accent | `#176447` | 버튼, 링크, 단계 번호 |
| Accent Hover | `#0F4F38` | 버튼 Hover |
| Highlight | `#D9F99D` | 작은 강조와 상태 표시 |
| Code | `#101713` | 프롬프트·배포 예시 배경 |
| Code Ink | `#E9F4EC` | 코드 영역 텍스트 |

### 대비 원칙

- 본문은 Canvas 위 Ink 또는 Ink Soft만 사용한다.
- Accent 배경 버튼에는 Accent Ink를 사용한다.
- Highlight는 장식과 짧은 라벨에만 사용하고 긴 본문 배경으로 사용하지 않는다.

## 3. 타이포그래피

### 글꼴

```text
본문: Pretendard → Noto Sans KR → Apple SD Gothic Neo → system-ui
코드: SFMono-Regular → Consolas → Liberation Mono → monospace
```

외부 폰트 요청 때문에 화면이 늦게 표시되지 않도록 시스템 폰트를 우선 사용한다.

### 크기

| 역할 | 크기 |
|---|---|
| Hero | `clamp(3.25rem, 9vw, 8rem)` |
| Section title | `clamp(2.5rem, 6vw, 5.5rem)` |
| Subheading | `clamp(1.75rem, 3vw, 2.5rem)` |
| Card title | `1.375rem` |
| Body large | `1.125rem` |
| Body | `1rem` |
| Label | `0.75rem` |

### 사용 규칙

- Hero와 큰 제목에는 `-0.055em` 자간을 사용한다.
- 본문 줄 높이는 `1.72`로 유지한다.
- 영문 Eyebrow는 대문자와 `0.16em` 자간을 사용한다.
- 한 문단의 최대 폭은 약 65자 수준으로 제한한다.

## 4. 레이아웃

| 항목 | 기준 |
|---|---|
| 최대 콘텐츠 폭 | `1180px` |
| 읽기 영역 | `760px` |
| 좌우 여백 | `clamp(20px, 4vw, 40px)` |
| 섹션 상하 간격 | `clamp(80px, 11vw, 144px)` |
| 내비게이션 높이 | `72px` |

### 데스크톱

- Hero는 넓은 제목과 설명을 비대칭으로 배치한다.
- 작업 흐름은 번호, 제목과 설명이 한눈에 비교되도록 구성한다.
- 요청 예시는 2열 카드 또는 큰 코드 패널로 표현한다.

### 모바일

- 모든 주요 레이아웃을 1열로 전환한다.
- 주요 CTA는 필요하면 화면 폭 전체를 사용한다.
- 최소 터치 영역은 `44px` 이상으로 유지한다.
- 제목의 의도적인 줄바꿈은 모바일에서 자연스럽게 재배치한다.

## 5. 컴포넌트 규칙

### Header

- 좌측 브랜드, 우측 섹션 링크와 GitHub 링크
- 스크롤 이동을 방해하지 않는 간결한 구조
- 모바일에서는 핵심 CTA와 메뉴 버튼만 표시

### Primary Button

- Accent 배경과 Accent Ink 텍스트
- 최소 높이 `48px`
- Pill 형태
- Hover 시 Accent Hover 적용
- 키보드 Focus 시 4px Focus ring 표시

### Secondary Button

- 투명 배경과 Ink 테두리
- Primary Button과 같은 높이·라운드 사용

### Workflow Card

- 단계 번호를 Accent 색상으로 표시
- 제목과 설명을 명확히 분리
- 테두리 또는 Surface 배경 사용
- 순서를 장식 아이콘에만 의존하지 않는다.

### Prompt Card

- Code 배경과 Code Ink 텍스트
- 실제 채팅 문장처럼 큰따옴표 없이 표시
- 복사 기능은 후속 개선사항으로 둔다.

### Deployment Status

- 상태 점과 `READY` 텍스트를 함께 표시
- 색상만으로 상태를 전달하지 않는다.

### FAQ

- 기본 HTML `details`와 `summary` 사용을 우선한다.
- JavaScript 없이도 열고 닫을 수 있어야 한다.

## 6. 모션

- 버튼과 카드 Hover: `160ms`
- 섹션 등장 효과: `260ms`
- 과도한 Parallax와 자동 재생 애니메이션은 사용하지 않는다.
- `prefers-reduced-motion` 사용자는 모션 시간을 `0ms`로 적용한다.

## 7. 접근성

- 키보드로 모든 링크와 버튼에 접근 가능해야 한다.
- Focus 스타일을 제거하지 않는다.
- 제목은 `h1 → h2 → h3` 순서를 지킨다.
- 장식 아이콘은 스크린 리더에서 숨긴다.
- 모바일 내비게이션 버튼에 명확한 이름을 제공한다.
- 링크 문구만 읽어도 이동 목적을 알 수 있게 작성한다.

## 8. 구현 파일

디자인 토큰은 다음 파일에서 관리한다.

```text
src/styles/design-tokens.css
```

4일차 메인화면 구현에서 이 파일을 `globals.css`에 불러와 모든 섹션에 적용한다.

## 9. 3일차 완료 기준

- [x] 디자인 콘셉트 확정
- [x] 브랜드 색상 정의
- [x] 제목·본문·코드 글꼴 정의
- [x] 글자 크기와 줄 높이 정의
- [x] 여백과 최대 너비 정의
- [x] 버튼·카드·상태·FAQ 규칙 정의
- [x] 모바일 반응형 기준 정의
- [x] 접근성 및 모션 원칙 정의
- [x] 재사용 가능한 CSS 디자인 토큰 작성
