# 새 기획 1일차 — 방향 전환 및 구조 정리

## 1. 프로젝트 목표

> Codex와 채팅으로 홈페이지의 주제, 디자인과 내용을 수정하고, 자동 검증을 통과한 결과를 GitHub와 Vercel에 배포한다.

이 저장소는 고객이 직접 홈페이지를 생성하는 SaaS가 아니라, Codex가 실제 홈페이지를 제작하고 지속적으로 수정하는 **홈페이지 원본 템플릿 및 자동 배포 작업 환경**으로 사용한다.

## 2. 운영 흐름

```text
사용자 채팅 요청
→ Codex가 홈페이지 수정
→ lint·typecheck·build 검사
→ GitHub main에 커밋·Push
→ Vercel 자동 배포
→ 운영 URL에서 결과 확인
```

## 3. 현재 홈페이지의 역할

- 현재 메인화면은 새 홈페이지의 임시 베이스 템플릿으로 유지한다.
- 새로운 업종과 콘텐츠가 확정되기 전에는 메인화면 디자인을 삭제하거나 전면 변경하지 않는다.
- 2일차에 실제 홈페이지 주제, 상호명, 서비스와 CTA를 확정한 후 콘텐츠를 교체한다.

## 4. 기능 분류

### 유지

| 대상 | 이유 |
|---|---|
| `src/app/page.tsx` | 새 홈페이지 메인화면의 출발점 |
| `src/app/layout.tsx` | 공통 메타데이터와 레이아웃 |
| `src/app/globals.css` | 현재 디자인 베이스 |
| `src/app/error.tsx`, `not-found.tsx` | 기본 오류 처리 |
| `src/app/api/health/route.ts` | 배포 상태 확인 |
| Next.js·TypeScript·ESLint 설정 | 개발과 자동 검증 기반 |
| GitHub `main` 브랜치 | 변경 이력과 배포 기준 |
| Vercel Git 연동 | Push 이후 자동 배포 |

### 보류

아래 기능은 즉시 삭제하지 않고 다음 구조 정리 때 제거한다. 기존 작업을 확인해야 할 경우 Git 이력에서 복구할 수 있다.

| 대상 | 현재 판단 |
|---|---|
| `/sign-in`, `/sign-up`, `/auth` | 새 홈페이지에는 로그인 불필요 |
| `/dashboard` | 고객용 SaaS 관리 화면 불필요 |
| `/sites`, `/sites/new` | 홈페이지 생성 SaaS 흐름 불필요 |
| `/api/sites/**` | 사이트·페이지 CRUD API 불필요 |
| `src/lib/supabase/**` | 데이터베이스 사용 계획 없음 |
| `src/lib/auth/**` | 사용자 인증 사용 계획 없음 |
| `src/proxy.ts` | 보호 경로 제거 후 불필요 |
| `supabase/migrations/**` | 새 운영 구조에서 사용하지 않음 |
| `@supabase/ssr`, `@supabase/supabase-js` | 관련 코드 제거 후 의존성 제거 |

### 문서 보관

- 기존 1~10일차 SaaS 문서는 과거 기획 기록으로 보관한다.
- 새로운 작업은 `Codex 채팅형 홈페이지 제작 일차별 계획서.md`를 기준으로 한다.
- 기존 문서는 새 개발 범위의 지시사항으로 사용하지 않는다.

## 5. 대표 저장소와 배포 주소

### GitHub

```text
Repository: https://github.com/cursaman/autosites
Production branch: main
```

### Vercel 대표 프로젝트

```text
Project: autosites-jd3d
Production URL: https://autosites-jd3d.vercel.app
Git repository: cursaman/autosites
Branch: main
```

### 중복 프로젝트

```text
Project: autosites
URL: https://autosites-drab.vercel.app
상태: 정리 대기
```

중복 프로젝트는 아직 삭제하지 않는다. 대표 프로젝트의 자동 배포와 운영 주소가 안정적으로 동작하는 것을 확인한 뒤 Git 연동 해제 또는 프로젝트 삭제를 별도 승인받아 진행한다.

## 6. 새 MVP 범위

### 포함

- 실제 업종에 맞춘 반응형 원페이지 홈페이지
- Codex를 통한 디자인·문구·이미지 수정
- 기본 SEO와 접근성
- 자동 코드 검사
- GitHub 변경 이력
- Vercel 자동 배포와 운영 확인

### 제외

- 홈페이지 생성 SaaS
- 회원가입과 로그인
- Supabase 데이터베이스
- 사용자별 사이트 관리
- 결제와 구독
- 고객용 편집기
- 고객별 자동 프로젝트 생성

## 7. 안전한 정리 순서

1. 현재 상태를 GitHub에 보존한다.
2. 2일차에 실제 홈페이지 콘텐츠를 확정한다.
3. 새 메인화면에 필요한 파일을 먼저 구현한다.
4. 빌드와 운영 화면을 검증한다.
5. 불필요한 SaaS 경로와 Supabase 의존성을 제거한다.
6. 대표 Vercel 프로젝트만 남긴다.

## 8. 1일차 완료 기준

- [x] 새로운 프로젝트 목적 확정
- [x] 유지·보류·제거 후보 분류
- [x] 기존 SaaS 문서와 새 계획 구분
- [x] 대표 GitHub 저장소 지정
- [x] 대표 Vercel 프로젝트와 운영 URL 지정
- [x] 중복 Vercel 프로젝트 확인
- [x] 현재 메인화면 보존 결정
- [ ] 실제 홈페이지 업종과 콘텐츠 확정 — 2일차
