# 7일차 — 사용자 권한 및 접근 제어

## 목표

- 로그인 여부는 Proxy에서 빠르게 확인한다.
- 실제 데이터 권한은 페이지와 Supabase RLS에서 다시 확인한다.
- 모든 사이트 데이터는 `owner_id`를 기준으로 고객별로 격리한다.
- 존재하지 않는 사이트와 권한이 없는 사이트를 모두 404로 처리한다.

## 구현 항목

- 공통 `requireUser()` 서버 인증 함수
- `/sites`, `/sites/[siteId]` 보호 화면
- `profiles`, `sites` 테이블 마이그레이션
- 사이트 조회·생성·수정·삭제 소유자 RLS 정책
- 신규 인증 사용자 프로필 자동 생성 트리거
- 소유권이 없는 사이트 ID의 정보 노출 방지

## Supabase 적용 방법

Supabase Dashboard의 SQL Editor에서 아래 파일을 실행한다.

```text
supabase/migrations/202608310001_day07_authorization.sql
```

## 권한 구조

| 대상 | 비로그인 | 로그인한 소유자 | 다른 사용자 |
|---|---:|---:|---:|
| 공개 페이지 | 허용 | 허용 | 허용 |
| 대시보드 | 차단 | 허용 | 본인 화면만 허용 |
| 사이트 목록 | 차단 | 본인 데이터만 허용 | 조회 불가 |
| 사이트 상세 | 차단 | 허용 | 404 처리 |
| 사이트 변경 | 차단 | 허용 | DB 정책에서 차단 |

## 완료 기준

- [x] 보호 경로 인증 검사
- [x] 서버 페이지 사용자 재검증
- [x] 사용자별 사이트 RLS 작성
- [x] 권한 없는 리소스 정보 노출 방지
- [x] 신규 사용자 프로필 자동 생성
- [ ] Supabase에 마이그레이션 적용
- [ ] 사용자 A가 사용자 B의 사이트를 조회할 수 없는지 통합 테스트
