# 초기 작업환경 준비 및 연결 가이드

AutoSites 방식으로 작업하려면 아래 순서로 한 번만 준비합니다.

```text
ChatGPT·Codex → Git → GitHub → Vercel → Supabase(선택)
```

## 각 서비스의 역할

| 서비스 | 역할 | 필요한 이유 |
| --- | --- | --- |
| Codex | 홈페이지 제작·수정·검사 | 채팅 요청을 React·Next.js 코드와 콘텐츠 변경으로 반영합니다. |
| Git | 버전과 변경 이력 관리 | 작업 내역을 기록하고 문제가 생기면 이전 정상 버전으로 되돌립니다. |
| GitHub | 소스코드 온라인 저장 | 코드를 보관하고 Vercel 자동 배포의 시작점이 됩니다. |
| Vercel | 프론트엔드·백엔드 배포 | Next.js 화면과 API를 서버에서 실행하고 도메인·SSL·배포를 관리합니다. |
| Supabase | 로그인·데이터베이스·CRUD | 회원 인증과 데이터 생성(Create)·조회(Read)·수정(Update)·삭제(Delete)를 처리합니다. |

GitHub Pages도 정적 React 홈페이지는 공개할 수 있지만, 현재처럼 Next.js 백엔드 API까지 운영하려면 Vercel 같은 서버 실행 환경을 사용합니다. Supabase는 모든 홈페이지에 필수는 아니며 로그인이나 저장할 데이터가 있을 때 연결합니다.

## 1. ChatGPT 가입과 Codex 준비 — 필수

1. [ChatGPT](https://chatgpt.com)에 가입하고 로그인합니다.
2. [공식 Quickstart](https://learn.chatgpt.com/docs/quickstart)의 안내에 따라 ChatGPT 데스크톱 앱을 설치합니다.
3. 데스크톱 앱에서 홈페이지 프로젝트 폴더를 엽니다.
4. 작업 도구로 Codex를 선택하고 `이 프로젝트 구조를 확인해줘`처럼 첫 요청을 보냅니다.

확인 기준: Codex가 프로젝트 파일 목록과 현재 홈페이지 구조를 읽을 수 있어야 합니다.

## 2. Windows에 Git 설치 — 필수

PowerShell에서 설치합니다.

```powershell
winget install --id Git.Git -e --source winget
```

설치 후 터미널을 완전히 닫았다가 다시 열고 확인합니다.

```powershell
git --version
git config --global user.name "YOUR_NAME"
git config --global user.email "YOUR_EMAIL"
```

`YOUR_NAME`과 `YOUR_EMAIL`은 본인의 GitHub 이름과 이메일로 바꿉니다. 다른 설치 방법은 [Git for Windows 공식 안내](https://git-scm.com/install/windows)를 참고합니다.

## 3. GitHub 가입과 저장소 생성 — 필수

1. [GitHub](https://github.com/signup)에 가입하고 이메일 인증을 완료합니다.
2. 우측 상단 `+` → `New repository`를 선택합니다.
3. 저장소 이름을 정하고 공개 범위를 선택합니다.
4. 로컬에 홈페이지 파일이 이미 있다면 README, `.gitignore`, License를 추가하지 않은 빈 저장소로 만듭니다.
5. 홈페이지 폴더에서 아래 명령을 실행합니다.

```powershell
git init -b main
git add .
git commit -m "first commit"
git remote add origin https://github.com/USERNAME/REPOSITORY.git
git push -u origin main
```

`USERNAME`과 `REPOSITORY`는 생성한 저장소 주소로 바꿉니다. 인증 창이 열리면 GitHub 계정으로 승인합니다. 자세한 내용은 [GitHub 공식 연결 안내](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github)를 따릅니다.

확인 기준: GitHub 저장소의 `main` 브랜치에 로컬 파일이 보여야 합니다.

## 4. Vercel 가입과 GitHub 자동 배포 — 필수

1. [Vercel](https://vercel.com/signup)에 GitHub 계정으로 가입합니다.
2. GitHub 저장소 접근 권한을 승인합니다.
3. Vercel 대시보드에서 `Add New` → `Project`를 선택합니다.
4. 방금 만든 GitHub 저장소 옆의 `Import`를 누릅니다.
5. Next.js 프로젝트라면 `Framework Preset`이 `Next.js`인지 확인합니다.
6. 일반적인 단일 프로젝트는 Root Directory를 `./`로 둡니다.
7. 필요한 환경변수가 없다면 바로 `Deploy`를 누릅니다.
8. 배포 상태가 `READY`가 되고 운영 URL이 열리는지 확인합니다.

이후 `main` 브랜치에 Push하면 Vercel이 자동으로 새 배포를 만듭니다. 설정 방식은 [Vercel Git 배포 공식 안내](https://vercel.com/docs/git)를 참고합니다.

## 5. Supabase 가입과 연결 — 선택

현재 AutoSites 소개 홈페이지처럼 공개 콘텐츠만 보여주는 사이트에는 Supabase가 필요하지 않습니다. 다음 기능이 생길 때 연결합니다.

- 회원가입과 로그인
- 문의·예약 데이터 저장
- 관리자 화면에서 콘텐츠 관리
- 사용자별 데이터 조회

연결 절차:

1. [Supabase Dashboard](https://database.new)에서 프로젝트를 만듭니다.
2. 데이터베이스 비밀번호를 안전하게 보관합니다.
3. 프로젝트의 `Connect` 화면에서 Project URL과 Publishable key를 확인합니다.
4. 로컬 프로젝트의 `.env.local`에 등록합니다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=프로젝트_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=Publishable_KEY
```

5. Vercel 프로젝트의 `Settings` → `Environment Variables`에도 같은 이름과 값을 등록합니다.
6. Production과 Preview 환경을 선택해 저장하고 다시 배포합니다.
7. 실제 테이블을 만들었다면 운영 전 RLS(Row Level Security) 정책을 반드시 확인합니다.

주의: Secret key나 service role key는 `NEXT_PUBLIC_` 변수에 넣지 않고, 브라우저 코드나 GitHub에 올리지 않습니다. 자세한 연결 예시는 [Supabase Next.js 공식 Quickstart](https://supabase.com/docs/guides/getting-started/quickstarts/nextjs)를 참고합니다.

## 최종 점검

- [ ] Codex가 홈페이지 프로젝트 폴더를 읽는다.
- [ ] `git --version`이 정상 출력된다.
- [ ] GitHub `main` 브랜치에 파일이 올라가 있다.
- [ ] Vercel 배포 상태가 `READY`이고 운영 URL이 열린다.
- [ ] `main` Push 후 Vercel에 새 배포가 생성된다.
- [ ] Supabase는 실제 로그인·데이터 기능이 있을 때만 연결했다.

준비가 끝나면 평소 작업은 아래 흐름으로 단순해집니다.

```text
Codex에 수정 요청 → 코드 검사 → GitHub main Push → Vercel 자동 배포 → 운영 주소 확인
```
